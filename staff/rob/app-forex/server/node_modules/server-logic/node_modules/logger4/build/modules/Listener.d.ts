export declare class Listener {
    constructor();
    private _listeners;
    get listeners(): any;
    addListener(listenerId: string, callBack: any, key?: string | null): void;
    callListener(listenerId: string, params?: any[]): any[];
}
